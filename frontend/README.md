# SafeFlow AI

React + JavaScript + Vite frontend boilerplate.

## Run

```bash
npm install
npm run dev
```

The analysis service currently uses mock data. To connect the backend, set `USE_MOCK = false` in `src/services/analysisService.js`.
